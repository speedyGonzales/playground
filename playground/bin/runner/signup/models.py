from django.db import models
from django.contrib.auth import models
from django.utils.encoding import smart_text

# Create your models here.


class SignUp(models.User):
    #user = models.ForeignKey(SignUp)
    pass
